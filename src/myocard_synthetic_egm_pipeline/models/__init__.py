"""Shipped model cards — named tissue parameterisations.

YAML rather than Python so a parameterisation is a citable, diffable artifact
(see ``simulate/model_cards.py``). This package exists to give
``importlib.resources`` something to anchor on, so the cards resolve the same
way from a source checkout and from an installed wheel.
"""
